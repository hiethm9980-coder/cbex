// app_config.dart
//
// [Skeleton placeholder - mirrors hrportal structure]
// Responsibility: App/environment configuration: flavor detection (dev/prod) and base_url resolution (dev = fixed URL, prod = Firebase Remote Config).
// Port the implementation from hrportal/lib/core/config/app_config.dart when ready.
